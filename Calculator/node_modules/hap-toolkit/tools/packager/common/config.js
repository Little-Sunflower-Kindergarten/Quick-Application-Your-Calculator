'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.initOptions = exports.options = undefined;

var _utils = require('./utils');

var options = {
  // 是否编译复合选择器,生成_meta信息
  optimizeDescMeta: false,
  // 是否压缩CSS属性名
  optimizeCssAttr: false,
  // 是否压缩模板属性名
  optimizeTemplateAttr: false,
  // TODO:是否支持页面级别的样式(暂不支持)
  optimizeStylePageLevel: false,
  // TODO:是否支持应用级别的样式(暂不支持)
  optimizeStyleAppLevel: false

  /**
   * 初始化命令行中传递的配置
   * @param envOptions
   */
}; /*
    * Copyright (C) 2017, hapjs.org. All rights reserved.
    */

function initOptions(envOptions) {
  for (var key in envOptions) {
    options[(0, _utils.hyphenedToCamelCase)(key)] = envOptions[key];
  }
}

exports.options = options;
exports.initOptions = initOptions;