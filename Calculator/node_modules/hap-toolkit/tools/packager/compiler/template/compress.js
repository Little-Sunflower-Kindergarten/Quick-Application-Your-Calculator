'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
/*
 * Copyright (C) 2017, hapjs.org. All rights reserved.
 */

// 模板属性名全写-缩写对照表
var templateAttrMap = {
  'type': 't',
  'attr': 'a',
  'classList': 'cL',
  'style': 's',
  'events': 'e',
  'children': 'c'

  /**
   * 压缩模板属性名
   * @param jsonObject
   */
};function compressTemplateAttr(jsonObject) {
  if (!jsonObject) {
    return;
  }

  if (jsonObject.children) {
    for (var i = 0, len = jsonObject.children.length; i < len; i++) {
      var child = jsonObject.children[i];
      compressTemplateAttr(child);
    }
  }

  for (var k in jsonObject) {
    if (templateAttrMap[k]) {
      var kAbbr = templateAttrMap[k];
      jsonObject[kAbbr] = jsonObject[k];
      delete jsonObject[k];
    }
  }
}

exports.compressTemplateAttr = compressTemplateAttr;